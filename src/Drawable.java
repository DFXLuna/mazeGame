import java.awt.Graphics;

/**
 * An interface for drawable things. This is probably not necessary, and may
 * be replaced by something more robust in the future.
 */
public interface Drawable {
  public void draw(Graphics g);
}
